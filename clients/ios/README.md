# clients/ios

Native SwiftUI iOS client, structurally mirroring `clients/android` as
closely as the two platforms allow (same screen breakdown, same `Prefs`
key names/defaults, same `GbaButton`/`ExtButton` data model) rather than a
from-scratch redesign.

## Why this needs a real Mac (or macOS CI runner)

Unlike every other client here, this one **cannot be built or even
syntax-checked on Linux at all**. `devkitPro` (3DS/Switch/NDS) and `emsdk`
(Web) are real cross-compiler toolchains that run fine here; Xcode and the
SwiftUI/UIKit frameworks are Apple-proprietary and only ship for macOS —
there's no Linux port, not even for `import SwiftUI` to resolve. The Swift
*language* itself has an official Linux toolchain (swift.org), but that
only covers server-side code (Foundation etc.), not the UI frameworks this
client is built on.

Practical consequence: this directory's code is written and reviewed here,
but the *only* real verification loop is the `ios` job in
`.github/workflows/build.yml`, which runs on a `macos-14` GitHub Actions
runner (the same kind of runner Cemu/dolphin-gba-stream's own CI already
uses for their macOS builds). No local "does it compile" feedback exists
before that job runs — budget for slower iteration than every other client
in this repo.

No code signing is configured (`CODE_SIGNING_ALLOWED: NO` in
`project.yml`) since CI only ever builds for the Simulator, which needs
none. A real device build or App Store submission needs an Apple Developer
account and isn't set up here.

## Structure

No `.xcodeproj` is committed (binary-ish, unreviewable diffs) — instead
`project.yml` is an [XcodeGen](https://github.com/yonaskolb/XcodeGen) spec;
`xcodegen generate` (CI's first step) turns it into a real project. Same
reasoning as `clients/switch`'s CMakeLists.txt not committing a generated
`build/`.

- `Sources/App/` — app entry point (`@main`).
- `Sources/Models/` — `Prefs`, `LocaleHelper`, `GbaButtons`,
  `ExtButtons`, `UnisonProtocolConstants` — direct ports of the
  equivalently-named Android files, kept structurally identical (same
  key names/defaults/bit values) rather than reinvented.
- `Sources/Views/` — SwiftUI views, one per Android Activity
  (`MenuView` ~ `MenuActivity`, etc.).
- `Sources/Resources/*.lproj/Localizable.strings` — generated by
  `i18n/generate.py` from `i18n/strings.json` (same single source of
  truth every other client uses) — do not edit by hand.
- `Tests/UnisonTests/` — XCTest, direct ports of the Android JVM unit
  tests for the same pure-logic pieces (`PrefsTest.kt` ->
  `PrefsTests.swift`, etc.).

## Phasing (this is not a finished client yet)

This first commit is a scaffold: project setup, CI pipeline, and the
pure-logic pieces (`Prefs`, `LocaleHelper`, button tables) with real tests
— proving the whole XcodeGen + `xcodebuild`-on-macOS-CI loop actually
works before investing in the much larger remaining pieces:

1. **Swift↔unison_core C bridge** — Android's `jni_bridge.c` is JNI- and
   MediaCodec-specific and isn't portable as-is; iOS needs its own thin
   bridge (Swift's native C interop via a module map, no JNI-equivalent
   glue needed for the protocol parsing itself) plus AVFoundation/
   VideoToolbox for video decode and AVAudioEngine for audio
   playback/capture, replacing MediaCodec/AudioTrack/AudioRecord.
2. **MenuView**: real UDP discovery-beacon listener + lobby/slot-picker
   flow (`MenuActivity.kt` is ~430 lines; the current `MenuView` is just
   the manual host:port form).
3. **PlayerView**: the actual streaming screen — touch overlay, physical
   keyboard/game-controller input via `GCController`, video rendering.
4. **SettingsView, LanguageView, VideoModeView, KeyBindingsView,
   TextInputView** — direct ports of their Android counterparts.
