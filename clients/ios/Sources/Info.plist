<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- XcodeGen (re)generates this entire file from project.yml's
         `info.properties` dict on every `xcodegen generate` -- this path
         is a write-target, not a source XcodeGen merges into, so nothing
         added here by hand actually survives a build (confirmed the hard
         way: a previous hand-edit here had zero effect on the built .ipa).
         CFBundleShortVersionString/CFBundleVersion in particular live in
         project.yml now, not here -- see that file's own comment. -->
</dict>
</plist>
